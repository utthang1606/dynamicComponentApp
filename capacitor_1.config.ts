import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'tan.test.ionic.starter.test',
  appName: 'dynamicComponentApp',
  webDir: 'www'
};

export default config;
